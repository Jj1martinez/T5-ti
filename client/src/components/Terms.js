import React from 'react';

const Character = (props) => {
    const { match: { params } } = props;
return (
  <div>
    <h2>{"Terms & Service "}</h2>
    My Home page!
  </div>
)
}

export default Character;